import { FormEvent,useState } from 'react'; import { Link } from 'react-router-dom'; import { authAPI } from '@/services/api'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { Label } from '@/components/ui/label'; import { Card,CardContent,CardHeader } from '@/components/ui/card';
export default function VerifyPendingPage(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('Check your inbox and follow the verification link.');const submit=async(e:FormEvent)=>{e.preventDefault();const{data}=await authAPI.resendVerification(email);setMsg(data.message);};return <main id="main-content" className="auth-page"><Card className="auth-card"><CardHeader><h1 className="text-2xl font-bold">Verify your email</h1><p className="text-sm text-muted-foreground">Verification unlocks learning, uploads, groups, and tutoring.</p></CardHeader><CardContent><form onSubmit={submit} className="space-y-4"><div><Label htmlFor="verify-email">Email address</Label><Input id="verify-email" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><Button>Resend verification</Button><p role="status" aria-live="polite">{msg}</p><Link className="text-primary underline" to="/login">Back to sign in</Link></form></CardContent></Card></main>}
